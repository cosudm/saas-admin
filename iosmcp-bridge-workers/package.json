{
  "name": "iosmcp-gateway-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IOS+ MCP Gateway Bridge — any API (REST/GraphQL/SOAP/gRPC) into governed MCP tools, on Cloudflare Workers + D1.",
  "scripts": {
    "build": "echo 'Gateway Bridge: worker + static console — nothing to build'",
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "predeploy": "npm run db:migrate:remote",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.88.0"
  }
}
